# ── Codespaces / local dev (Docker Postgres) ────────────────
POSTGRES_URL=postgresql://postgres:pass@localhost:5432/warranty_claims
NEXT_PUBLIC_USE_BLOB_UPLOAD=false

# ── Vercel production (auto-set when you add Vercel Postgres) ─
# POSTGRES_URL=          ← set automatically by Vercel Postgres integration
# BLOB_READ_WRITE_TOKEN= ← Vercel Dashboard → Storage → Blob
# NEXT_PUBLIC_USE_BLOB_UPLOAD=true